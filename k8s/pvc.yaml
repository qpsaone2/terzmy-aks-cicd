# k8s/pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blob-pvc
  namespace: dev
  labels:
    app: myapp
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""  # ← 빈 문자열
  volumeName: blob-pv-static
  resources:
    requests:
      storage: 10Gi