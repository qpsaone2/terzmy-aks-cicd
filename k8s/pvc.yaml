apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blob-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azureblob-fuse-premium
  resources:
    requests:
      storage: 10Gi
